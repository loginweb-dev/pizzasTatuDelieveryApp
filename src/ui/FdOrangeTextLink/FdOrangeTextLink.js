import React from 'react';
import { Text } from 'react-native';
import styles from './styles';

const FdOrangeTextLink = props => (
  <Text style={[styles.text_styles]}>
    {props.text}
  </Text>
);


export default FdOrangeTextLink;
